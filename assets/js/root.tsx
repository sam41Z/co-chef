import React from "react";

const Root: React.FC<void> = (props: void) => {
  return (
    <section className="phx-hero">
      <h1>Welcome to the Root with Typescript and React!</h1>
      <p>Peace-of-mind from prototype to production</p>
    </section>
  );
};

export default Root;
